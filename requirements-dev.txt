# Test dependencies
#
# On Debian/Ubuntu, install via apt instead of pip:
#   sudo apt install python3-pytest python3-pytest-asyncio
#
# Otherwise use pip:
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.1.0
pytest-mock>=3.10.0

# Code quality (optional)
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Runtime dependencies (also needed for tests)
-r requirements.txt
